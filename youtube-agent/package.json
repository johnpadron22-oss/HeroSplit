{
  "name": "herosplit-youtube-agent",
  "version": "1.0.0",
  "description": "YouTube automation agent integration for HeroSplit — turns workout programs into YouTube content",
  "main": "herosplit-agent.js",
  "type": "commonjs",
  "scripts": {
    "start": "node herosplit-agent.js",
    "setup": "node setup.js",
    "generate:content": "node herosplit-agent.js --action=generate",
    "generate:script": "node herosplit-agent.js --action=script",
    "publish": "node herosplit-agent.js --action=publish"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "axios": "^1.7.0",
    "sqlite3": "^5.1.6"
  },
  "notes": {
    "agent-source": "Requires the youtube-automation-agent repo cloned alongside this project (see AGENT_PATH in .env or defaults to ~/Desktop/youtube-automation-agent)"
  }
}
